---
# EXAMPLE SECRET VALUES — replace with real ones, then encrypt:
#   ansible-vault encrypt inventories/homelab/group_vars/homelab_postgresql/vault_backup.yml
#
# S3 credentials and the pgBackRest repository cipher passphrase for the
# backup bucket described in vars_backup.yml.

vault_postgresql_backup_s3_key: "EXAMPLE_S3_ACCESS_KEY"
vault_postgresql_backup_s3_key_secret: "EXAMPLE_S3_SECRET_KEY"
vault_postgresql_backup_cipher_pass: "changeme"
